name: Build VLauncher
on: [push, workflow_dispatch]
jobs:
  android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build APK with Buildozer Action
        uses: TechShreyash/buildozer-action@v2
        with:
          buildozer_version: 'stable'
